[project]
name = "arrowhead"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.16.0",
]

[tool.setuptools.packages.find]
where = ["src"]
